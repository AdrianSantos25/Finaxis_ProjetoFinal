# Configuração do Servidor
PORT=3000
NODE_ENV=development

# Base de Dados MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=gestor_financeiro
DB_PORT=3306

# Sessão (gere uma chave aleatória forte para produção)
SESSION_SECRET=

# SMTP (Email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
